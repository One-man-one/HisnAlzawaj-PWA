# حصن الزواج — PWA: ملاحظات للعمل على هذا المستودع

واجهةُ الويب وحدها: صفحاتٌ ساكنة تُثبَّت وتعمل بلا اتصال. الوصف في
`README.md` — وهذا الملفّ لما لا يظهر في القراءة العابرة: ما يُكسر
بسهولة، ولماذا اختير ما اختير.

## 🔴 اقرأ أولاً: المستودع **عامّ**

كلُّ ما يُدفَع إلى هنا يصير **منشوراً للعالم فوراً وإلى الأبد** — لا
يكفي حذفه لاحقاً، فالتاريخ يبقى.

**لا توكن. لا مفتاح API. لا كلمة سرّ. لا سلسلة اتصال بقاعدة.**

⚠️ وهذه ليست قاعدةً نظرية: المستودع كان خاصّاً حتى جُعل عاماً في ١٩
سبتمبر ٢٠٢٦ (بلا ذلك لا يعمل GitHub Pages في الخطة المجانية). فكلُّ
ما قبل ذلك التاريخ فُحص ونظيف — وما بعده مسؤوليةُ من يدفع.

⚠️ **وأخطرُ ما يُنسى تحديداً: رابط `getFile` من تيليجرام** — نصُّه
يحمل توكن البوت، فكشفُه في صفحةٍ يقرؤها المتصفّح تسليمُ البوت كلّه.
الصورُ تمرّ من الوسيط وسيطاً، لا برابطٍ مباشر.

✅ **وما يُدفع هنا عامٌّ على أي حال:** الصفحات تُنزَّل إلى متصفّح كل
زائر. فالسؤال ليس «هل أُخفيه؟» بل «هل يصلح أن يراه الناس؟» — وما لا
يصلح فموضعه الوسيط في مستودع البوت، لا هنا.

## ⚠️ واقرأ هذا قبل أي مرحلة

**الخطة في مستودع البوت: `docs/PWA_PLAN.md`** — ومعها **عقد العزل**،
وهو ما يمنع أن يتعطّل البوت العامل بسبب هذا المشروع. لا تبدأ مرحلةً
قبل قراءته، ولا تنسخ الخطة هنا (مصدرٌ واحد).

## ⚠️ القاعدة التي تُكسَر أولاً: رقم إصدار عامل الخدمة

`VERSION` في رأس `sw.js`.

عامل الخدمة يخدم من الكاش، فالمستخدم لا يرى نسخةً جديدة إلا حين يتغيّر
هذا الرقم. **ومن نشر تعديلاً ونسي رفعه يترك كل مستخدمٍ مثبِّتٍ للتطبيق
عالقاً على النسخة القديمة إلى الأبد** — لا يُصلحه تحديثُ الصفحة ولا
إعادةُ فتح التطبيق. والسيرفر يحمل الجديد، والمستخدم يرى القديم، ولا شيء
في أي سجلٍّ يشير إلى السبب.

✅ **كل دفعةٍ تمسّ ملفّاً في `SHELL` ترفع `VERSION`.**

## ⚠️ المسارات النسبيّة — ولا استثناء

كل مسارٍ في HTML وCSS وJS و`sw.js` و`manifest.webmanifest` **نسبيّ بلا
شرطة بادئة**: `app.css` لا `/app.css`، و`.` لا `/`.

✅ **والقاعدة أثبتت نفسها في ٢٢ سبتمبر ٢٠٢٦:** انتقل الموقع من مسارٍ
فرعي (`one-man-one.github.io/hisnalzawaj-pwa/`) إلى **جذر نطاقٍ خاصّ**
(`app.hisnalzawaj.com`) — **بلا تعديل سطرٍ واحد**. ولو كان فيه مسارٌ
مطلقٌ واحد لَانكسر عنده.

⚠️ **ولا تُحذف القاعدة لأن الاستضافة صارت على الجذر**: هي ما يجعل
النقلة التالية (نطاقٌ آخر، أو معاينةٌ في مجلّد فرعي، أو خادمٌ محلّي
على مسار) مجّانيةً كذلك. والعطل لا يشتكي مباشرةً: `cache.addAll` يفشل
بصمتٍ نسبيّ، فيعمل التطبيق ما دامت الشبكة ثم ينهار بلا اتصال وحده —
أي أن الميزة الوحيدة التي بُني لأجلها هي الوحيدة المعطَّلة.

## ⚠️ النطاق يسكن ملفّ `CNAME` لا لوحة GitHub وحدها

`CNAME` في الجذر يحمل `app.hisnalzawaj.com`، وهو ما يقرؤه GitHub Pages
عند كل نشر.

⚠️ **ودرسٌ كلّف انقطاعاً:** ضُبط النطاق من لوحة Pages **قبل** إنشاء
سجلّ DNS، فحفظه GitHub رغم `InvalidDNSError` — وتوقّف العنوان القديم
(404) ولم يعمل الجديد. **الترتيب: سجلّ DNS أوّلاً، ثم النطاق.**

⚠️ **وسجلّ `CNAME` في Cloudflare يجب أن يكون `DNS only`** (غيمة
رمادية): البروكسي يُخفي خوادم GitHub فلا يتحقّق من الملكية ولا
تُصدَر الشهادة — ولا رسالة تدلّ على السبب.

## ⚠️ لا ترجماتٍ مكتوبةً هنا

اللغات الستّ تعيش في `locales/` بمستودع البوت، وتصل هذه الواجهةَ **من
الوسيط**: `GET /api/i18n/{lang}` يعيد مفاتيح `web.*` وحدها (٤٥ مفتاحاً
اليوم) بلغةٍ واحدة، والناقص يرتدّ إلى العربية كما في البوت حرفياً.

✅ **وقد وُصلت فعلاً (٢٢ سبتمبر ٢٠٢٦):** كلُّ نصٍّ متغيّر في
`index.html` يحمل `data-i18n` ويُملأ من `ui.js`. ومن كتب نصّاً عربياً
مكان مفتاح فقد بدأ نسختين تتباعدان: مفتاحٌ يُضاف في البوت ولا يُضاف
هنا يعطي شاشةً مختلطة بلا أي خطأ يدلّ على السبب.

⚠️ **ولم يبقَ عربياً في الصفحة إلا ثلاثة أسطر، وهي استثناءٌ مقصود:**
اسمُ العلامة، وسطرُ «لحظة…»، وسطرُ «تعذّر الاتصال بالخادم» — لأنها ما
يُقرأ **قبل** أن تصل النصوص أو **حين لا تصل**. ومفتاحٌ مترجَم في
موضعها لا يُقرأ أبداً.

✅ **والنسخة في `localStorage` ليست استثناءً من القاعدة:** لا تُكتب في
مستودع ولا تُحرَّر بيد — هي ذاكرةُ آخر ما أرسله الوسيط. وبدونها تفتح
الشاشة **فارغة** بلا اتصال: عامل الخدمة يخزّن الصفحة والأنماط، ولا
يخزّن ردَّ الوسيط. ⚠️ والنصوص تُطلب في كل إقلاعٍ ولو وُجدت النسخة،
وإلا لم يصل تصحيحُ مفتاحٍ إلى من يحملها أبداً.

## ⚠️ الاتّجاه يتبع اللغة، لا الصفحة

`index.html` يبدأ `dir="rtl"` لأن العربية هي الأكثر — و`ui.js` يصحّحه
عند الإقلاع بحسب اللغة (`ar` و`fa` يمنةً، وما سواهما يسرةً).

⚠️ ومن حذف ذلك السطر لن يرى شيئاً يسقط: الشاشة سليمة، **والقارئ
الفرنسيّ وحده هو المقلوب** — أزرارٌ وقوائم من اليمين في واجهةٍ
فرنسية.

## ⚠️ `[hidden]` لا تكفي وحدها

الشاشاتُ هنا تُظهَر وتُخفى بسمة `hidden`، وقاعدةٌ مثل
`.sheet { display: flex }` **تتغلّب** على `display: none` التي يعطيها
المتصفّح للسمة. وقع هذا فعلاً: ورقةُ التفاصيل وإعلانُ التطابق كانا
يفتحان فوق الشاشة من أوّل لحظة. ولذلك في رأس قسم التطبيق من `app.css`
قاعدةٌ صريحة: `[hidden] { display: none !important; }`.

## ⚠️ ولا سكربت من طرفٍ ثالث — ولا واحد

التوكن يسكن `localStorage` (الشرح في رأس `api.js`)، وكلُّ سكربتٍ يعمل
في الصفحة يقرؤه. ولذلك لا CDN ولا أداةَ تحليلات ولا زرَّ «سجّل
بجوجل» جاهزاً: تلك كلُّها ملفّاتٌ مستضافة عند صاحبها تتحدّث بلا إذن.

✅ **والدخول بحسابٍ قائم يُبنى بإعادة توجيهٍ لا بسكربت** — الخطة
كاملةً في `docs/PWA_PLAN.md` ← «المرحلة ٦ ب».

## أعراف

**بلا إطار عمل وبلا خطوة بناء، بقرار صريح.** الملفّات تُنشر كما هي —
نفس نمط `site/` و`webapp/` في مستودع البوت. من أضاف أداةَ بناءٍ أضاف
معها `node_modules` وقفلَ تبعياتٍ وخطوةَ نشرٍ تفشل وحدها، مقابل صفحةٍ
لا تحتاج شيئاً من ذلك.

**الألوان رموزٌ على `:root`،** والنمط الداكن يُعيد تعريفها في موضعٍ
واحد. لونٌ مكتوبٌ مباشرةً في قاعدةٍ ما يبقى فاتحاً في الداكن بلا أن
يشتكي شيء.

**لا خطّ ويبٍ مُنزَّل.** خطّ النظام العربي حاضرٌ على كل منصّة، وخطٌّ
عربي كامل يعني ~٣٠٠ كيلوبايت قبل أوّل حرفٍ يظهر — على شبكةٍ بطيئة هو
الفرق بين تطبيقٍ يفتح وشاشةٍ بيضاء.

**الأيقونات مولَّدة لا محرَّرة.** `tools/make_icons.py` هو المولّد،
ومصدرُها **الشعار الرسمي** `brand/logo_mark.png`. وتحريرُ PNG بيدٍ
يضيع عند أوّل إعادة توليد. وهي **تُرفَع** رغم ذلك: الاستضافة ساكنة
بلا خطوة بناء، فما لا يُرفع لا يوجد.

⚠️ **والشعار نسخةٌ لا رابط:** أصله `assets/logo_mark.png` في مستودع
البوت، وهذا المستودع لا يملكه وقت البناء. فتحديثُ الشعار هناك **لا
يصل هذه الأيقونات من نفسه** — انسخ الملفّ ثم `python3
tools/make_icons.py`. وهي نفس قاعدة `webapp/birthdate.html` هناك:
نسختان تتباعدان ما لم ينتبه أحد.

⚠️ **والألوان تتبع الشعار لا الذوق:** `--brand` و`--accent` في
`app.css` مأخوذان بالقياس من `brand/logo_mark.png` (`#1C3834`
و`#C99D61`). وكانا تخميناً قبل ذلك فبدت الواجهة قريبةً من الهوية لا
منها — فرقٌ يراه صاحب العلامة في أوّل نظرة ولا يراه من كتب الكود.

**الاتجاه `rtl` والصفحة عربية،** والتخطيط يعمل على أضيق هاتف بلا تمرير
أفقي.

## ⚠️ التثبيت يختلف بالمتصفّح — ملاحظةٌ من جهازٍ حقيقي

- **كروم (أندرويد)** — يُطلق `beforeinstallprompt`، ويُنتج **WebAPK**:
  تطبيقٌ مسجَّل في النظام، بلا شارةِ متصفّحٍ على أيقونته.
- **فايرفوكس (أندرويد)** — **لا يُطلق الحدث إطلاقاً**، فزرُّ التثبيت
  لا يظهر. والتثبيت من قائمته يُنتج **اختصاراً** تحمل أيقونتُه شارة
  فايرفوكس.
- **سفاري (iOS)** — لا حدث ولا زرّ. «مشاركة ← إضافة إلى الشاشة
  الرئيسية» وحدها.
- **نسخ Chromium الأخرى** — بعضها يُطلق الحدث ثم **يتجاهل `prompt()`
  بصمت**، ولا يحسم `userChoice` أبداً.

⚠️ **ولذلك الدليل اليدويّ ظاهرٌ دائماً ولا يُخفى خلف فشلٍ يُنتظر:**
في ثلاثٍ من هذه الأربع لا يصل المستخدم إلى الزرّ أصلاً، فمن ربط ظهور
الدليل بفشل الضغطة تركهم بلا طريق.

## التوثيق

هذا المشروع يوثّق **لماذا** لا **ماذا**، وبالعربية. علامة ⚠️ تسبق ما
يُكسر بسهولة أو ما جرّبه أحدهم فوقع. حافظ على هذا الأسلوب — التعليقات
هنا جزء من الكود لا زينة فوقه.
